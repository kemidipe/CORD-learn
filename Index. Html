<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1.0" />
<title>C.O.R.D — Connecting Opportunities, Resources & Dreams</title>
<style>
  :root {
    --primary: #0077ff;
    --accent: #00e1ff;
    --bg1: #001d3d;
    --bg2: #003566;
    --text: #ffffff;
  }
  * {margin:0;padding:0;box-sizing:border-box;font-family:'Poppins',sans-serif;}
  body {
    min-height:100vh;
    background:linear-gradient(135deg,var(--bg1),var(--bg2));
    color:var(--text);
    display:flex;
    flex-direction:column;
    align-items:center;
    justify-content:center;
    overflow-x:hidden;
  }
  h1 {
    font-size:2.5rem;
    text-align:center;
    animation: glow 2s ease-in-out infinite alternate;
  }
  @keyframes glow {
    from {text-shadow:0 0 10px var(--primary);}
    to {text-shadow:0 0 25px var(--accent);}
  }
  p {
    max-width:600px;
    text-align:center;
    margin:20px;
    font-size:1.1rem;
    opacity:.9;
  }
  .buttons {
    display:flex;
    flex-wrap:wrap;
    justify-content:center;
    gap:15px;
    margin-top:20px;
  }
  .btn {
    padding:12px 24px;
    border:none;
    border-radius:30px;
    background:linear-gradient(45deg,var(--primary),var(--accent));
    color:white;
    font-size:1rem;
    cursor:pointer;
    transition:.3s;
  }
  .btn:hover {
    transform:translateY(-3px) scale(1.05);
    box-shadow:0 0 20px var(--accent);
  }
  footer {
    position:absolute;
    bottom:20px;
    text-align:center;
    font-size:.9rem;
    opacity:.7;
  }
</style>
</head>
<body>
  <h1>Welcome to <span style="color:var(--accent)">C.O.R.D</span></h1>
  <p>Connecting <strong>Opportunities</strong>, <strong>Resources</strong>, and <strong>Dreams</strong>.<br>
  Learn, Teach, Invest — all in one network that helps everyone grow.</p>

  <div class="buttons">
    <button class="btn" onclick="alert('Student section coming soon!')">Join as Student</button>
    <button class="btn" onclick="alert('Teaching portal coming soon!')">Teach a Skill</button>
    <button class="btn" onclick="alert('Investor hub coming soon!')">Invest in Ideas</button>
  </div>

  <footer>© 2025 Dipe Oluwafikunmi Olaoluwa</footer>
</body>
</html>
      
